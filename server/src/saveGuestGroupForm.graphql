type SaveGuestGroupFormPayload {
  id: ID!
}

extend type Mutation {
  saveGuestGroupForm(
    id: ID!
    guests: Json!
    accomodation: Boolean!
    transport: Boolean!
    comments: String
  ): SaveGuestGroupFormPayload
}
