type AddGuestsPayload {
  id: ID!
}

extend type Mutation {
  addGuests(
    weddingId: ID!
    name: String!
    allowAccomodation: Boolean!
    guests: Json!
    customGreeting: String
  ): AddGuestsPayload
}
